class Helpers {
  static String formatWelcomeMessage(String userName) {
    return 'Hello, \$userName! Welcome back!';
  }

  static String formatDate(DateTime date) {
    return '\${date.day}/\${date.month}/\${date.year}';
  }

  // Add more helpers or utility methods here.
}
